apiVersion: apps/v1
kind: Deployment
metadata:
  name: maas-api
spec:
  replicas: 1
  template:
    spec:
      serviceAccountName: maas-api
      securityContext:
        runAsNonRoot: true
      containers:
      - name: maas-api
        image: maas-api
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
          name: http
          protocol: TCP
        - containerPort: 9090
          name: https-metrics
          protocol: TCP
        env:
        - name: NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        - name: SECURE
          value: "false"
        - name: LOG_FORMAT
          value: "otel-json"
        - name: MAAS_SUBSCRIPTION_NAMESPACE
          value: "models-as-a-service"
        - name: METRICS_PORT
          value: "9090"
        - name: METRICS_SECURE
          value: "false"
        # METRICS_CERT_DIR defaults in Go (DefaultMetricsCertDir); set explicitly in overlays/tls.
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: true
          runAsNonRoot: true
          seccompProfile:
            type: RuntimeDefault
        volumeMounts:
        - name: metrics-certs
          mountPath: /tmp/k8s-metrics-server/metrics-certs
          readOnly: true
      volumes:
      - name: metrics-certs
        secret:
          secretName: maas-api-metrics-tls
          defaultMode: 420
          # optional in base/dev (no service-ca). overlays/tls sets optional: false.
          optional: true
      terminationGracePeriodSeconds: 30
